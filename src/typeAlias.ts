// type person = {
//   name: string;
//   age: number;
//   profession: string;
//   address: string;
// };

// const personOne: person = {
//   name: "Putin",
//   age: 58,
//   profession: "President",
//   address: "Russia",
// };
// const personTwo: person = {
//   name: "Biden",
//   age: 65,
//   profession: "President",
//   address: "USA",
// };

// type marriedType = boolean;
// const isMarried: marriedType = false;

// type courseType = string;
// const courseName: courseType = "Next Level Web Development";

// type operationType = (x: number, y: number) => number;

// // const calculate = (x:number, y:number, operation:(x:number, y:number) => number) => {
// //     return operation(x, y);
// // }

// const calculate = (x: number, y: number, operation: operationType) => {
//   return operation(x, y);
// };

// calculate(12, 33, (x, y) => x + y);
// calculate(12, 33, (x, y) => x - y);
// calculate(12, 33, (x, y) => x * y);
// calculate(12, 33, (x, y) => x / y);
